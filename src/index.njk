---
layout: layouts/base.njk
title: Vienna Ski Weekends
description: "Your guide to the best weekend ski trips from Vienna. Expert-tested ski resorts within 1-4 hours of the Austrian capital."
---
<section class="hero">
  <div class="hero__content">
    <h1 class="hero__title">Weekend Ski Trips from Vienna</h1>
    <p class="hero__tagline">Expert-tested Austrian ski resorts, all within a few hours of the city. Plan your next powder day.</p>
  </div>
</section>

{% set postList = collections.posts %}

{% if postList.length %}
<section class="featured" aria-label="Featured article">
  {% set latest = postList[0] %}
  <article class="featured__card">
    {% if latest.data.heroImage %}
    <div class="featured__image">
      <a href="{{ latest.url | url }}">
        <img src="{{ latest.data.heroImage | url }}" alt="{{ latest.data.heroImageAlt or latest.data.title }}" loading="eager" width="800" height="420">
      </a>
    </div>
    {% endif %}
    <div class="featured__body">
      <h2 class="featured__title">
        <a href="{{ latest.url | url }}">{{ latest.data.title }}</a>
      </h2>
      <p class="featured__excerpt">{{ latest.data.description or (latest.content | excerpt) }}</p>
      <time class="featured__date" datetime="{{ latest.date | htmlDateString }}">{{ latest.date | readableDate }}</time>
    </div>
  </article>
</section>
{% endif %}

{% set remainingPosts = postList | limit(999) %}
{% if remainingPosts.length > 1 %}
<section class="post-grid" aria-label="All articles">
  <h2 class="post-grid__heading">Latest Articles</h2>
  <div class="post-grid__list">
    {% for post in remainingPosts %}
      {% if loop.index0 > 0 %}
      <article class="post-card">
        {% if post.data.heroImage %}
        <div class="post-card__image">
          <a href="{{ post.url | url }}">
            <img src="{{ post.data.heroImage | url }}" alt="{{ post.data.heroImageAlt or post.data.title }}" loading="lazy" width="400" height="210">
          </a>
        </div>
        {% endif %}
        <div class="post-card__body">
          <h3 class="post-card__title">
            <a href="{{ post.url | url }}">{{ post.data.title }}</a>
          </h3>
          <p class="post-card__excerpt">{{ post.data.description or (post.content | excerpt) }}</p>
          <time class="post-card__date" datetime="{{ post.date | htmlDateString }}">{{ post.date | readableDate }}</time>
        </div>
      </article>
      {% endif %}
    {% endfor %}
  </div>
</section>
{% endif %}
